# Limitations Register

Landing zone for the spec §11 conversion rule: when the eye flags something, it
becomes either a **new validation check** or a **logged known-limitation here** —
never a tweak-look-tweak loop. Each entry: what, status, source.

| # | Limitation | Status | Source |
|---|------------|--------|--------|
| 1 | Canals / engineered channels appear "below grade" because the DTM captures engineered canal beds. Real, not a bug; fluvial output masks channel cells. | Known / accepted | HANDOFF §6 |
| 2 | Manila raingrid pluvial produces a domain-wide thin sheet (wet-threshold vs denoise-threshold mismatch). Out of Singapore scope; do not trust Manila pluvial numbers. | Known / open | HANDOFF §8.1 |
| 3 | WRI Aqueduct comparator is ~1 km resolution vs the 150 m hotspot hit-radius — a documented asymmetry, scored as-is (not resampled away). | Known / accepted | spec §7 |
| 11 | **§11 visual gate — speckle item converted (eye → number).** Pluvial fields read visually as speckly. Quantified (RP100, ssp585_2100): at the 0.05 m denoise threshold **0%** of wet area is in <6-cell clusters and **92%** is in ≥20-cell coherent pools — the apparent speckle is real sub-hectare ponding (1,758 clusters of 6–19 cells = 0.5–1.8 ha), not noise. A ~3% cosmetic sub-6 fragment appears only at the 0.10 m *display* threshold (denoise runs at 0.05 m). **Converted to:** (a) a numeric anti-speckle check (≥85% of wet area in ≥20-cell clusters at the denoise threshold — currently 92%, PASS); (b) this logged characteristic. Not a veto. Optional cosmetic: align denoise threshold to the 0.10 m hit threshold. | Known / accepted (converted) | visual gate 2026-06-01 |
| 3b | **Coastal is a no-defence/no-pumping bathtub upper bound** (RP100 = 66.5 km²; atlas ~25× over-prediction vs documented present-day). Defended-DEM run (Marina Barrage + ECP bund) changes extent by only 0.1–0.2 km²/RP — crests aren't overtopped but are localised polylines; the over-prediction is intrinsic to bathtub-on-30m-DSM with no pumping. Read as screening upper bound (plausibility tier). Option: local-inertial re-run (~1.4× SG reduction). **Re-run assessed 2026-06-02 and deliberately DEFERRED:** coastal carries no comparative claim (plausibility-tier only), the ~25× over-prediction is already honestly framed as a screening upper bound, and a local-inertial re-run (high effort) would shave it only to ~18× — changing no claim in the paper. Not worth the build for the thesis; revisit only for product use. | Known / accepted (re-run deferred, rationale logged) | defended run 2026-05-31; deferral 2026-06-02 |
| 4 | Aqueduct has **no pluvial layer** (riverine + coastal only). Its score on a pluvial hotspot table reflects "generic vendors omit pluvial by design", not a modelling failure. | Known / accepted (it IS the thesis) | spec §7.1, HANDOFF §3 |
| 5 | Naive TWI baseline is RP-independent (pure topography) and depends on a `flag_fraction` threshold (default 15% wettest land); it cannot represent rainfall magnitude. Governed by spec §4.5 param-history. | Known / accepted | spec §7.2 |
| 6 | Hotspot-table coordinates are georeferenced **area centroids** (not surveyed flood points); confidence flagged per row. At a 150 m hit radius, `low`-confidence points can flip a hit — refine under spec §4.5. | Known / open | spec §6, table `georef_confidence` |
| 7 | pysheds 0.5 calls `np.in1d`, removed in numpy ≥2.0; a module-level `np.in1d = np.isin` shim is required for flow accumulation in this environment (also affects `model/hand_model.py` accumulation usage). | Known / accepted (env) | `scripts/build_naive_pluvial_baseline.py` |
| 8 | **Raingrid pluvial high-RP blow-up — RESOLVED (interior).** First run: RP200 = 27.8 m, non-monotonic, from unbounded accumulation in DSM-artefact pits. Surgical de-pitting (spec 2026-05-31; fill sub-sea-level + ≥ threshold depressions) removed it: monotonic, HWM in-band, hit-rate held. | **Resolved** | `build_conditioned_dem.py --raingrid-out`; re-run 2026-05-31 |
| 8b | **Residual raingrid boundary artefact — RESOLVED.** After the de-pit, RP1000 = 3.76 m (clipped-domain edge, 23 cells; confirmed not a pit). Fixed by an open-boundary condition in `run_rain_on_grid` (`open_boundary=True`: outer ring of finite cells drains) → RP1000 3.22 m. | **Resolved** | open-boundary fix 2026-05-31 |
| 9 | **Inconsistent pluvial forcing across scenario hazard-levels CSVs.** Pluvial net-excess (water_level_m): ssp585_2020 0.057 m, ssp585_2100 0.089 m (both ~physically plausible for 1 h IDF) vs ssp585_2050 0.462 m, ssp245_2050 0.447 m, ssp245_2100 0.479 m (~0.46 m = 460 mm excess, physically impossible for a 1 h storm; ~5× too high). Also inverted: ssp585_2100 (ΔT=4 °C) < ssp585_2050 (ΔT=1.5 °C). Pre-existing data bug in committed CSVs (mixed fit/scaling/drain-capacity provenance). **Pluvial scenario-grid comparison invalid for the 2050/245 files; coastal grid is clean.** Headline ssp585_2100 + baseline-2020 use plausible forcing. Fix: regenerate all hazard-levels with one consistent pluvial fit. **RESOLVED — verified 2026-06-02:** all five hazard-level CSVs now carry consistent, monotone pluvial forcing (RP100 water_level_m: 2020 0.057 → 245_2050 0.074 → 585_2050 0.077 → 245_2100 0.080 → 585_2100 0.089 m — increasing with scenario severity, no inversions, ~physically plausible) and the scenario-grid RASTERS were regenerated from them (RP100 max depth 2.58→2.91 m, p99.9 0.78→1.04 m, monotone across scenarios; common mtime). The §5.5 scenario-grid pluvial comparison is now valid across all four scenarios. | **Resolved** | scenario grid 2026-06-01; regen verified 2026-06-02 |
| 10 | **Comparative claim: model loses to naive-TWI only at the pre-registered single point; dominates across the operating space.** Diagnostics (§5.1.2): model beats TWI at RP100 (TSS 0.53 vs 0.47) and at RP50 for any threshold ≤ 0.075 m (0.68 vs 0.47); 9/20 hotspots are near-misses (0.05–0.10 m) at the 0.10 m cutoff; model uniformly more specific (CRR 0.83 vs 0.67). Event-RP verification (§6.2): documented storms are ~RP5–50 (1 h), NOT RP100 → re-anchoring scoring-RP NOT licensed (kept). Under-flagging partly explained by drainage-FAILURE events (PUB: choked Stamford Canal 2010) that a design-capacity model cannot reproduce. Resolution: report full operating curve + specificity (not single-point); claim reframed to "beats generic vendor; more specific than naive topography." **SUPERSEDED by #12:** the "dominates across the operating space" finding held only on the earlier 20-point subset; on the full 38-point official register the model does NOT out-locate topography at any operating point (and all ΔTSS CIs span zero). The surviving claims are specificity + significant vendor win, not operating-curve dominance. | **Superseded by #12** | diagnostics 2026-06-01; superseded by register expansion 2026-06-01 |
| 12 | **Comparative verdict separates onto two axes — location (tie) vs specificity (model wins).** Register expanded both arms: positives 20→38 (full official PUB list, OneMap-geocoded + 2 historical), dry controls 6→20 (elevated reserves + low-lying town centres absent from the comprehensive PUB list, OneMap-geocoded + DEM-verified, model-blind selection). Result on the present-day field (paired bootstrap, n=10 000): (a) **combined TSS — indistinguishable**, every model-vs-TWI ΔTSS spans zero (RP50/0.10 m −0.18 [−0.48, 0.13]; RP50/0.05 m +0.04 [−0.26, 0.34]; RP100/0.10 m −0.14 [−0.45, 0.16]); (b) **specificity — model SIGNIFICANTLY better**: TWI floods 14/20 documented-dry points (CRR 0.30) vs model 7/20 (CRR 0.65), paired ΔCRR +0.35 [+0.10, +0.60], P=0.99, **CI excludes zero**. Expanding the dry set crashed TWI's own TSS (0.59→0.22), exposing its indiscriminate flooding. **Consequence:** no location-skill ranking is supportable (TSS tie), but the specificity edge (the model's hypothesised value, §6.3) is now demonstrated, plus the significant vendor win. **Conversion:** CIs reported with every TSS/CRR (§4.3, §5.1.2c–d); both arms now reasonably powered. | Known / characterised (claims sharpened) | bootstrap 2026-06-01; register expansion (positives + dry controls) 2026-06-02 |
| 6b | **Hand-typed dry-control coordinates are unreliable — do not expand the register by eye.** An attempt to add 6 elevated dry controls from memory failed a DEM elevation cross-check: 2/6 were mis-georeferenced (Bukit Gombak 26 m vs expected ~125 m; Pasir Panjang east 14 m vs ~50 m), and small isolated hills (Fort Canning 48 m, Pearl's Hill 37 m) flag anyway because the 150 m hit-radius catches the wet base. The additions were reverted rather than pollute the CRR. **Any N growth must use geocoded/surveyed coordinates, not manual pins.** | Known / accepted (process) | dry-control expansion attempt 2026-06-01 |
| 8c | **Residual interior solver overshoot — RESOLVED.** After open boundary, 3 interior cells (0.0001 %) overshot to 3.22 m (local-inertial transient overshoot, not pit/edge). Closed by a physical depth cap at the documented 3.0 m engineering limit (`run_rain_on_grid peak_depth_cap_m`, `run_multihazard --pluvial-depth-cap`), applied after the cause-fixes — RP1000 = 3.00 m, gate 1–2 **PASS**. | **Resolved** | depth cap 2026-06-01 |
| 13 | **Event-based reproduction (data-limited to ~4 documented events).** Driving the present-day field and checking the documented historical events at their event-matched RP: the *capacity-exceedance* events reproduce (Tanglin 2011 and Bukit Timah 2012/2017 flag at RP25/RP50), but the *drainage-failure* events under-reproduce (Orchard 2010/2011 reach only 0.09–0.14 m vs documented 0.3–0.4 m). This is mechanistically consistent with §6.2: a design-capacity model structurally cannot reproduce blockage-driven floods (choked Stamford Canal). Documented depths (0.3–0.4 m) are reached only at higher RP / climate-inflated forcing, confirming the model is conservative at present-day event magnitudes. A richer event hit-rate is blocked by SG's small documented-event sample (most reports are qualitative, not metric). | Known / characterised | event-reproduction 2026-06-02 |
| 14 | **Documented flood-depth ground truth is sparse (sourcing limit).** Only ~2 SG events carry geocodable metric depths (Orchard ~0.4 m; Bukit Timah ~0.25 m, ~0.70 m in large-drain areas); the depth-realism/HWM check rests on these. A wider search (2026-06-02) found only qualitative descriptions ("knee-deep", "waist-level") elsewhere. More metric depths would strengthen §5.3 but are not available; not a model defect. | Known / accepted (data limit) | depth search 2026-06-02 |
| 15 | **Specificity advantage is operating-point- and negative-set-confounded.** The "model significantly more specific than TWI" result (ΔCRR +0.35 curated / +0.55 random at the 0.10 m threshold; #12) is confounded by *flooded fraction*: at 0.10 m the model wets only 1.85% of land vs TWI's 15% by construction, so it rejects more negatives partly because it floods less, at far lower sensitivity (HR 0.39 vs 0.92). Fair tests overturn the blanket claim: (a) at **matched specificity** (curated CRR 0.65) TWI hit-rate 0.71 > model 0.39; (b) **threshold-free ROC-AUC** on the decision-relevant low-lying negatives is a **tie** — model 0.65 vs TWI 0.75, paired ΔAUC −0.10, 95% CI [−0.32, +0.11] (TWI nominally higher, NOT significant). The model's **only firm** threshold-free edge is **vs random land**: paired ΔAUC +0.14, 95% CI [+0.04, +0.24] at n=300, point estimate +0.12 to +0.28 and CI excluding zero in 5 of 6 random samples (seeds 1/2/3/7/2026) — better landscape-scale detection, NOT better low-vs-low discrimination. Surfaced by the question "why isn't the low-lying flooding?" → small net-excess forcing (design-capacity assumption: ~4 cm at RP50, 0 below ~RP5) → conservative model. **Consequence:** downgrade "significantly more specific" to a scoped detection edge; the SESMO practice note is rebuilt around this fragility. **Conversion:** caveats added to dossier §5.1.2 and full manuscript §4.2; ROC-on-both-negative-sets + matched-operating-point now the recommended comparison. | Known / characterised (claim downgraded) | operating-point/negative-set analysis 2026-06-03 |
| 16 | **KL scenario hazard-level CSVs carry mixed-provenance pluvial forcing (limitation #9 recurring in KL).** The committed scenario-forcing guard (`scripts/validate_scenario_forcing_consistency.py`, flood-v2.0) FAILs for `kuala_lumpur` with 27 problems: (a) 18 cap violations — pluvial `water_level_m` of 0.563–1.169 m at RP25+ across `hazard_levels_ssp245_2050/ssp585_2050/ssp245_2100.csv` (vs the IDF-calibrated plausible ~0.03–0.12 m); (b) 9 monotonicity inversions where `ssp245_2100` > `ssp585_2100` at every RP, with `ssp585_2100 = ssp245_2100 × 0.149` exactly — a constant-ratio scaling defect making the most-severe scenario 6–7× too low. `ssp585_2100` is the **clean** IDF-calibrated file (RP100 = 0.122 m); the three others carry the buggy values. **Impact scoped:** the present-day baseline (Task 7, `--delta-T 0.0` from `hazard_baseline_template.csv`) is UNAFFECTED, and the deliverable's headline future scenario (SSP5-8.5 2100) is the clean file. **Fix:** regenerate all KL hazard-levels with one consistent pluvial fit (Plan 3 task). **Conversion (eye→number):** the guard is the committed numeric check; this row is the logged known-limitation. **RESOLVED (2026-06-06):** the 3 stale files' pluvial rows were regenerated as `clean present-day baseline (ssp585_2020) × GEV-CC(1+cc_rate·delta_T)` — the exact `build_hazard_levels._gev_cc_factor(…, hydraulic_exponent=1.0)` linear-CC rescale (verified to reproduce the clean ssp585_2100 from the baseline). Fluvial/coastal rows were already consistent → untouched. **Guard: 27 problems → PASS**; pluvial now monotone, within the 0.50 m cap, and correctly ordered ssp585_2020<ssp245_2050<ssp585_2050<ssp245_2100<ssp585_2100 at every RP. `scripts/_regen_kl_scenario_pluvial.py`. NOTE: scenario FLUVIAL rows still carry the pre-Plan-7 (un-bias-corrected) GLoFAS stages; the 2.06× fluvial bias + main-stem HAND (Plan 7/8) apply when scenario RASTERS are generated (the SSP5-8.5 2100 deliverable), not to the forcing CSV here. **PROPAGATED to the delta cities (2026-06-10):** the same bug was present in `bangkok`, `bangkok_chao_phraya`, and `jakarta` (ssp245_2050/ssp585_2050/ssp245_2100 pluvial ~0.40–0.80 m, the ssp245_2100>ssp585_2100 inversion). The KL formula did NOT transfer (their climate response is gentler — anchor ratio 1.113 vs KL's 1.28), so the fix was generalised to **additive interpolation of each config's own documented anchor pair** (present-day baseline → its clean ssp585_2100) at each scenario's ΔT (`scripts/_regen_scenario_pluvial.py`) — anchor-exact, ssp585_2100 (the paper's Table II cell) UNTOUCHED, handles the base=0 low-RP excess. **Guard now PASSES for all five configs** (bangkok/bangkok_chao_phraya/jakarta/singapore/kuala_lumpur); Singapore was already clean. The full SSP×horizon pluvial forcing grid is now consistent. | Resolved (KL + delta cities; guard PASS all configs) | #16 KL fix 2026-06-06; delta-city propagation 2026-06-10 |
| 17 | **Extent-CSI is non-viable for KL (SAR blind to urban flash flooding).** The MYS2021 GFM SAR composite holds only 345 flood pixels (~0.14 km²) across the KL bbox (raw tiles mostly nodata over the dense urban Klang Valley); the UNOSAT FL20220112MYS vector covers Pahang/Johor, not the Klang Valley. A CSI against a 0.14 km² reference is meaningless, so KL is validated like Singapore (urban flash-flood city): documented-hotspot hit-rate primary + point-depth + IDF-anchor. Extent-CSI is run as a caveated diagnostic only. **Conversion:** hotspot hit-rate is the committed numeric gate (Plan 2 Task 3); this row is the logged limitation. | Known / accepted (SAR limit) | KL SAR sparsity check 2026-06-05 |
| 18 | **Raingrid pluvial solver is pathologically slow at high RP.** The KL capped re-run took ~30–45 min per RP and rising (rp_2→rp_50 spanned 13:43→16:01), so a full 9-RP city run is ~5–6 h — driven by the adaptive CFL timestep shrinking to ~0.4 s as deep water accumulates, pushing each high-RP solve toward its per-RP step cap. This is the **critical-path blocker for the multi-city goal** (every re-run and every new city pays it). Stopgap: `scripts/_apply_pluvial_depth_cap.py` post-hoc-clips existing rasters (mathematically identical to the in-pipeline cap) to avoid a full re-run. **Plan 4 attempt — post-storm convergence early-stop — REJECTED (negative result, 2026-06-05).** Mirroring the inertial solver's early-stop, `run_rain_on_grid` was given a mean-\|Δd\| convergence stop after the storm ends. On real KL terrain it **under-floods by 10–50%** (worst at low RP) — because the post-storm settling phase is NOT idle: it is water slowly routing into deep closed depressions to reach their true peak. Skipping it computes a wrong, under-flooded map. Worse, the under-flooded run *passed the hotspot gate harder* (CRR 0.71→0.86) by rejecting more dry controls — a trap that trusting the gate alone would have shipped. The unit test (single bowl) passed at 0.011 m only because one low dominated the mean; on KL's 3.9M-cell domain a few deep lows still filling don't move the mean, so convergence fires prematurely. **Lesson: do NOT early-stop the settling tail — it does real peak-filling work; a mean-of-domain metric is unsafe at scale.** Branch `raingrid-perf` discarded; this row is the surviving record. **Perf remains OPEN.** Next safe lever (deferred, not yet attempted): parallelise the 9 independent RP solves (separate processes) — a wall-clock win with zero change to the answer. **MITIGATED (Plan 10, 2026-06-06):** the safe lever is implemented. Benchmark found the solver is memory-bandwidth-bound — only **1.73× at 6 threads** (29% efficiency) — so each solve wastes ~4 cores. `model/raingrid_parallel.py` runs the independent per-RP solves in a `ProcessPoolExecutor` with **1 numba thread/worker** (`--raingrid-workers`, default auto=min(cores,#RPs); `1`=serial), reclaiming the wasted cores. The `prange` loops are element-wise → output is **BIT-IDENTICAL** (proven: unit test + real KL grid serial-vs-pool max|diff|=0.0). Projected **~2.6× on the 9-RP batch** (5–6 h → ~2 h); NOT yet measured end-to-end (a 2-RP test is overhead-dominated — per-process JIT + spawn; the win needs ≥~6 RPs saturating cores) — to be confirmed on the next full-city run. Early-stop stays FORBIDDEN; this is pure scheduling. | Mitigated (RP-parallel pool; bit-identical; 9-RP speedup pending real confirmation) | Plan 10 parallelisation 2026-06-06 |
| 19 | **KL pluvial broad-shallow over-extent — DIAGNOSED + partially fixed (drainage); residual: infinite-sink over-drain.** Root cause: the raingrid drained only at major rivers + sea (1.1% of domain; median RP100 wet cell 4.7 km from any outlet) — KL's urban drain network was unrepresented, so water ponded broadly in local lows (the dry-control false-positives, e.g. Bukit Antarabangsa's base). NOT depth (the cap was irrelevant). **Fix (Plan 5):** densified the outlet network with OSM waterways (real mapped drains/ditches/canals, culverts excluded; roads deliberately excluded as they flood) → median distance 4.7 km→190 m; burned into the raingrid DEM + wired as outlets. Result: over-extent −42%, Bukit Antarabangsa drains, **TSS 0.42→0.50 with CI now excluding zero (first significant skill)**. **Residual / new finding:** the model treats every waterway cell as an INFINITE sink, so flood-prone spots adjacent to a drain (Old Klang Road, by Sungai Klang) over-drain to 0.00 m → HR 0.71→0.65 (< floor) → gate FAIL-on-HR. Accepted as the improved baseline (user); a clean PASS is NOT chased by density-tuning (forbidden loop). Also: Taman Sri Muda (epicenter) is a fluvial/compound miss (deferred fluvial-RP issue), not pluvial. **Plan 6 (capacity-limited drains) — RESIDUAL IS STRUCTURAL.** Implemented per-channel finite drains (sea + major rivers = perfect sinks via `--major-river-raster`; minor drains = finite `--drain-conveyance-m-s`, anchored to MSMA secondary-drain Q → 0.002 m/s). Result ≈ identical to infinite-sink (extent +0.9 km², gate unchanged, Old Klang Road still 0.00 m; rivers did NOT over-pond = per-channel correct). **At the documented drain capacity, drains shed faster than inflow → not overwhelmed → Old Klang Road not restored.** Flooding it would need a sub-documented (blockage) conveyance = tuning to an undocumented blockage (forbidden). **Conclusion: KL's pluvial specificity ceiling (HR 0.65 / CRR 0.86 / TSS 0.50, significant) is partly STRUCTURAL — a design-capacity open model cannot reproduce blockage-driven floods (cf. #10/#13).** Finite-drain mechanism kept as a correct DEFAULT-OFF capability. **RESOLVED via the FLUVIAL path (Plan 7/8):** Old Klang Road sits on the Klang/Kerayong floodplain — it is a *fluvial* spot, not a blockage-pluvial one. The documented 2.06× GLoFAS bias + main-stem HAND (see #20) floods it 0.60 m. The pluvial blockage ceiling stands, but Old Klang Road is no longer an open HR gap. | Resolved (fluvial); pluvial blockage ceiling structural | Plan 5/6 (pluvial) + Plan 7/8 (fluvial resolution) 2026-06-06 |
| 20 | **Single-stage HAND fluvial must be referenced to the MAIN-STEM trunk the discharge represents — not channel-initiation, not raw OSM rivers (TRANSFERABLE rule).** HAND applies one basin-wide overbank stage to every cell within `stage` of its nearest channel. Two failure modes found on KL: (a) **raw OSM rivers** include hillside rivulets, so a 60–77 m hill (Bukit Persekutuan / Federal Hill) sat 0–2 m above a ~60 m tributary and flooded 4 m at the Klang main-stem stage (false positive, CRR 0.86→0.71); (b) **channel-initiation accumulation network (1.8 km²)** is spatially complete, so a uniform stage floods every valley within 6 m of any channel → **875 km² (25% of domain)** RP100 extent at 4 m mean depth — physically absurd despite scoring the *highest* gate (TSS 0.80). **The old HAND's "bounded" 46–116 km² extent was itself an artifact of incomplete OSM coverage (~20% of domain had a mapped river → NaN elsewhere), never a principled floodplain.** **Fix:** reference HAND to flow-accumulation channels at the **catchment scale of the modeled discharge**. KL GLoFAS = ~500 km² upper Klang basin (`cities.py`), so threshold = **catchment ≥ 180 km² (thr=200000 px)** → trunk Klang/Gombak/Kerayong only → `hand_mainstem_utm47n.tif`. Result: extent 875→**100 km²** (credible), Federal Hill **fixed by physics** (HAND 32 m), all documented positives preserved (Segambut via pluvial); **HR 0.76 / CRR 0.86 / TSS 0.62** (dominates the Plan-7 old-HAND 0.76/0.71/0.48). **Residual:** single-stage HAND extent is sensitive to the trunk threshold (no single threshold is both complete and bounded) — the 180 km² choice is forward-anchored to the documented discharge scale, gate as consistency check. **Also config (Plan 7):** the hazard CSV already encodes overbank above the documented Q_bf — `run_multihazard --fluvial-bankfull-rp` MUST be **0** for v2.0 CSVs (the default 10 double-subtracts). **TRANSFER to Bangkok/Jakarta:** build their HAND from accumulation channels at the GLoFAS-reach catchment scale, NOT raw OSM rivers or channel-initiation. **Taman Sri Muda** (Dec-2021 epicentre) remains an out-of-domain miss — 7.6 km from the nearest modeled HAND floodplain cell (channel-network coverage / retention-pond-failure), not a discharge problem. | Known / characterised (main-stem rule adopted; transferable; threshold-sensitivity is the structural limit) | Plan 7 bias + Plan 8 main-stem HAND 2026-06-06 |
| 21 | **KL dry-control negatives: clean LOW-LYING hard negatives are unattainable (low-lying ≈ flood-prone); the elevated set is geographically appropriate, corroborated by a systematic DIAGNOSTIC.** The primary gate's 7 dry controls are all elevated hills, risking the SG #15 easy-negative confound. Plan-9 tried to harden with low-lying negatives but research showed KL low-lying areas are pervasively flood-prone (Cheras/Setapak/Sri Petaling/Gombak/OUG/Taman Desa all have flood history) → named low-lying "dry" labels would mislabel. Instead a **systematic model-blind sampler** (`build_systematic_dry_controls.py`) picked 12 urban-valley points (main-stem HAND 6.5–20 m = above RP100 fluvial stage but below hilltops; not on channel; >1 km from 35 documented flood points). **Result:** model floods 5/12 (raw CRR 0.58), BUT ≥4 of the 5 are **documented flood areas** (Semarak 2020 worst-hit; Jinjang retention ponds; OUG; Puchong) — mislabels, model CORRECT; only 1 (Zon Perindustrian, 0.17 m, borderline at 0.10 m threshold) is an unexplained shallow FP. **The hard-negative test CORROBORATES specificity** (model floods valley sites that genuinely flood) rather than undermining it. **Disposition (user): DIAGNOSTIC not gate** — `kind=dry_diagnostic` excluded from the scored loader (`load_hotspots_from_manifest` scores only positive+dry); primary gate stays clean **HR 0.76 / CRR 0.86 / TSS 0.62, PASS**. The contaminated raw CRR is a lower bound (mislabels game CRR down). **Transfer note:** for delta cities (Bangkok/Jakarta) with extent-CSI, hard negatives are easier; for flash-flood cities, prefer documented-positive HR + elevated negatives + this diagnostic. | Known / characterised (elevated set appropriate; diagnostic corroborates; n=12 diagnostic recorded) | Plan 9 systematic hard-negative diagnostic 2026-06-06 |
| 22 | **Bangkok 2011 fluvial is OUT-OF-DOMAIN-SOURCED — an in-domain forcing+topology model cannot reach the northern districts (STRUCTURAL HR ceiling; quantifies the old Bangkok "single-reach" issue #8).** The 2011 megaflood entered the 5,443 km² model domain from the **north** as an overland sheet: the Chao Phraya's **160,000 km² catchment** overflowed far upstream (Ayutthaya), beyond any in-domain forcing. **Three B2 fluvial methods tested, all hit the same wall:** (a) **B1 dense OSM-river HAND** floods the CBD khlongs (HAND≈0) → CRR 0.43; (b) **hydrodynamic riverine** (inertial solver, per-cell WSE BC) is **intractable** — thin-film `q/h` CFL collapse along the domain-spanning channel (dt→0; ~10⁶ steps for 8 h) — **and** sustained-WSE equilibrium over-floods the flat, highly-connected delta (915k cells at t=0.09 h); (c) **trunk-only HAND (acc≥100000) on the DEFENDED DEM** (King's Dyke burned, excludes CBD khlongs) fixes CRR (0.43→**0.86**) but D8 routing **cannot connect the flat northern out-of-domain districts** to the in-domain trunk → they go NaN → HR 0.25 fluvial-alone. **The connectivity that protects the CBD is the same connectivity that disconnects the northern flood.** **Composite (pluvial ∨ trunk-HAND-fluvial ∨ coastal), defended DEM, RP100: HR 0.56 / CRR 0.86 / TSS 0.42 [0.04, 0.75]** — vs B1 0.62/0.43/0.05: a no-skill "flood-everything" model converted to **significant discriminative skill + strong specificity**. **The HR gate fails (0.56 < 0.70) and this is an HONEST STRUCTURAL CEILING, not a tuning target** — the ~7 missed positives (Don Mueang/Sai Mai/Pak Kret/Nonthaburi belt) are unreachable by any in-domain model. Per the **cardinal rule**, the flooded dry controls + missed positives **STAY** in the register. **Future lever (deliberately NOT forced):** inject the out-of-domain flood as a **north-boundary inflow** (documented 2011 stage as a BC at the northern domain edge) + tractable routing. The hydrodynamic riverine solver mode (`model/inertial_wave_model.py` array BC, TDD'd) + runner (`build_riverine_fluvial.py`, head anchored to documented 1.5–3 m 2011 floodplain depth) are kept as a recorded capability. **TRANSFER note for Jakarta/Manila/HCMC:** for cities whose validation event is sourced from an out-of-domain mega-catchment, HR is structurally bounded — set expectations from the catchment/domain ratio, validate CRR + skill, and document the source-boundary limit rather than tuning. | Known / characterised (structural HR ceiling; out-of-domain source; CRR+skill validated) | Plan B2 cross-method fluvial 2026-06-09 |
| 23 | **Jakarta J1 first gate: dense single-stage HAND over-broadens → CRR fail (the KL #20 fix applies; not yet built).** Existing-model present-day RP100 (subsidence DEM, inertial coastal, fillspill pluvial, dense Ciliwung `hand_utm48s.tif`): **HR 0.88 (14/16) / CRR 0.29 (2/7) / TSS 0.16 [−0.19,0.57]** — FAIL on CRR. Per-spot: the fluvial layer applies a uniform 4.30 m RP100 overbank via the DENSE single-stage HAND, flooding the central-levee dry controls (Menteng/Gambir 4.30 m) + elevated Cipete (3.27 m) — 4 of 5 flooded dry controls. Pluvial over-ponds elevated Jagakarsa (1.77 m at 53 m; KL Bukit-Antarabangsa pattern) = the 5th. **Fix (J2, documented, NOT yet built): main-stem HAND (#20)** referenced to the Ciliwung GloFAS-reach (Depok, ~370 km²) catchment scale. Jakarta should transfer BETTER than Bangkok #22 because the GloFAS point is near/in-domain (sub-basin anchor like KL Shah Alam), not out-of-domain. Flooded dry controls STAY (cardinal rule). **J2 UPDATE (2026-06-09): main-stem HAND does NOT transfer (premise overturned).** Viability sweep (`_diag_jakarta_mainstem_hand.py`): no accumulation threshold separates the Ciliwung-corridor positives from the central-levee controls — ≤90 km² reaches all 5 positives but extent GROWS to 711 km² (>existing 375) and Menteng/Gambir still flood; ≥180 km² spares controls but loses ALL positives (HAND 12–24 m). Cause = Ciliwung headwaters out-of-domain at Bogor → trunk shifts off the natural river (Bangkok #22 pattern, milder; **2/3 cities now confirm single-stage HAND fails on out-of-domain-fed flat deltas**). The actual CRR culprit was a **register mislabel**: Menteng/Gambir sit ON the corridor (HAND 0–2 m) and are documented flooded 2007/2013 (central Jakarta) → reclassified to positives (model-blind, flood-record-anchored; KL #21 pattern), dry set restored with 3 elevated-south controls. **Revised gate: HR 0.89 / CRR 0.50 / TSS 0.39 [0.03,0.75]** — no-skill→significant-skill, CRR 0.29→0.50, still FAIL. Residual = fill-spill pluvial over-ponding on elevated south (Jagakarsa/Pondok Pinang/Pasar Minggu, 3 of 4 FPs; **fix = KL Plan 5 drainage-densification, deferred J3**) + dense-HAND fluvial (Cipete, unfixable by main-stem HAND here). Flooded genuine controls STAY. | Known / characterised (main-stem HAND doesn't transfer; CRR mislabel corrected; residual = pluvial over-ponding, J3 lever) | Plan J1 + J2 2026-06-09 |
| 24 | **Jakarta coastal forcing is QUALITATIVE + rob under-reaches; pluvial IS IDF-calibrated (provenance gap closed).** (a) Coastal: no UHSLC gauge in Jakarta Bay → Muis et al. (2016) screening values (±0.2–0.3 m at RP100); coastal RP100 floods only Pluit/Muara Baru of 5 North-Jakarta rob positives (Penjaringan under-reached) — a documented confidence limit, not a tunable defect. (b) Single-reach: only the Ciliwung is modeled; Jakarta's ~12 other rivers are unrepresented → Kemang (Krukut) missed. (c) RESOLVED-as-found: the committed Jakarta pluvial is a BMKG 6h IDF-calibrated Gumbel (RP2=85/RP100=175 mm, 2026-05-16), NOT raw ERA5-Land — only the `cities.py` docstring still describes the superseded ERA5-Land path (worth a docstring cleanup). | Known / characterised (coastal qualitative; single-reach; pluvial IDF-confirmed) | Plan J1 2026-06-09 |
| 25 | **Jakarta JKT2020 extent-CSI is a WEAK support gate — the SAR is partly urban-blind too.** Best CSI 0.07 (pluvial RP100, H 0.12, FAR 0.86; fluvial CSI 0.07, H 0.19, FAR 0.91). The EOS-ARIA Sentinel-1 proxy captures peri-urban/open-water flood but MISSES urban floods inside central-Jakarta SAR layover/shadow zones (obs note) → spatial mismatch with the DKI-core model (low POD) + SAR-inflated FAR. Jakarta's SAR is only a partial improvement over KL's #17 urban-blindness; the urban-focused hotspot gate is the meaningful one. Consistent with the prior bathtub-era JKT2020 (CSI 0.10/FAR 0.87). | Known / characterised (SAR partial urban-blindness; CSI demoted to weak support) | Plan J1 2026-06-09 |
| 26 | **Pluvial-method heterogeneity across cities — the gates are NOT apples-to-apples on the pluvial layer (homogeneity gap).** KL was migrated to the **raingrid** pluvial solver (+ OSM-waterway drainage densification, Plan 5) and its validated gate (HR 0.76/CRR 0.86/TSS 0.62) is a raingrid result. **Bangkok and Jakarta use the pipeline-default `fillspill`** (different algorithm: raingrid routes rain to an outlet network; fillspill fills/spills catchment depressions). Consequence: (a) cross-city pluvial comparison is not strictly like-for-like; (b) KL's drainage-densification fix is **raingrid-specific and does NOT port to fillspill**, so Jakarta's residual CRR FPs (fill-spill over-ponding on elevated south — Jagakarsa/Pondok Pinang/Pasar Minggu, the KL Bukit-Antarabangsa pattern) cannot be fixed by the KL lever. A per-city fillspill tweak was deliberately NOT applied (it would increase heterogeneity, violating the homogeneous-&-transferable cardinal rule). **Resolution path (deferred, methodology-level): migrate Bangkok+Jakarta to the KL-validated raingrid+OSM-drainage and re-validate all three on one method.** Surfaced by the Jakarta J2 "is J3 aligned?" check. **DIAGNOSED (2026-06-10, `_diag_jakarta_pluvial_overpond.py`):** Jakarta's residual elevated-south CRR FPs are **closed-depression fill on the hillside (DEM-pit / fill-spill over-concentration), NOT the KL missing-drainage mechanism** — Jagakarsa fills a 51 m hilltop closed depression to 1.77 m (12x the 0.149 m RP100 excess; 330 m surroundings median 57 m). There is no real waterway on a hilltop to be a 'missing outlet', so the raingrid+OSM-drainage migration would NOT drain it — the migration is therefore neither necessary nor a CRR fix, and is NOT pursued. The correctly-targeted, SOLVER-AGNOSTIC lever is DEM hydro-conditioning / pit-breaching (FABDEM/Lindsay; paper future-work #5), which removes spurious closed depressions uniformly across all cities. #26 stands as honestly-disclosed as-is heterogeneity. See `…/runs/2026-06-10-cross-city-synthesis.md`. | Known / characterised (homogeneity gap; migration deferred) | cross-city synthesis 2026-06-10 |
